# The deployment section should be excluded until
# it is explicitly included in module 5

deployment:
  dockerhub:
    branch: master
    commands:
      - curl -X POST -H 'Content-Type:application/json' --data '{"build":true}' "https://registry.hub.docker.com/u/srimant/docker-ci/trigger/63dd04d4-d328-416c-b3be-dfec604799e6/"


test:
  override:     
    - mocha