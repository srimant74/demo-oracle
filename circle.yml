# The deployment section should be excluded until
# it is explicitly included in module 5

deployment:
  dockerhub:
    branch: master
    commands:
      - curl -X POST -H 'Content-Type:application/json' --data '{"build":true}' "https://registry.hub.docker.com/u/srimant/srimant-cci/trigger/9a38ed87-4079-4f52-9dd7-5755e0a74f72/"	  
test:
  override:     
    - mocha